apiVersion: v1
kind: Secret
metadata:
  name: cinescale-secrets
  namespace: cinescale
type: Opaque
stringData:
  postgres-password: postgres
  minio-access-key: minioadmin
  minio-secret-key: minioadmin
  database-url: postgresql+asyncpg://postgres:postgres@postgres:5432/cinescale
